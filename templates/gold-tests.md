# Gold Tests: {feature}

{In the user's active language: gold tests are add-only end-to-end journeys that prove user value for the selected scope mode.}

## GT-1: {name}

**Business journey**: {In the user's active language: what this proves}
**Scope mode**: `{scope}`
**Evidence class**: {class}
**User-facing runtime path**: {In the user's active language: UI/API/tool/orchestrator/CLI/service path where the user or system performs this journey}
**Covers phases**: {In the user's active language: phases}

**Steps**

1. {In the user's active language: step}
2. {In the user's active language: step}

**Expected outcome**

- {In the user's active language: outcome}

**Acceptance proof**

- `{command}` exits 0

## GT-2: {name}

**Business journey**: {In the user's active language: what this proves}
**Scope mode**: `{scope}`
**Evidence class**: {class}
**User-facing runtime path**: {In the user's active language: UI/API/tool/orchestrator/CLI/service path where the user or system performs this journey}
**Covers phases**: {In the user's active language: phases}

**Steps**

1. {In the user's active language: step}

**Expected outcome**

- {In the user's active language: outcome}

**Acceptance proof**

- `{command}` exits 0
