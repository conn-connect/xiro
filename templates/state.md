# Xiro State: {feature}

## Current Claim

{In the user's active language: what can honestly be claimed now:}

- {In the user's active language: claim, for example that the project contract exists or that spec and execution contracts are generated}

{In the user's active language: what cannot yet be claimed:}

- {In the user's active language: missing proof, for example that implementation has not started, no acceptance proof has passed, or runtime reachability has not been verified}

{In the user's active language: strongest evidence class achieved}: `{none | design-fixture | mock-contract | local-integration | runtime-compose | real-provider | manual-production | cannot-verify}`

{In the user's active language: safe to continue automatically}: `{yes | no}`

{In the user's active language: next recommended command}: `{none | /xiro spec <feature> | /xiro run <feature> | /xiro salvage <feature>}`

## Current Work

- Phase: {In the user's active language: phase or state that none is selected}
- Slice: {In the user's active language: slice id or state that there is none}
- Worker assignment: {In the user's active language: role/id or state that there is none}
- Runtime reachability: {In the user's active language: verified path, unverified path, or state that it has not been checked yet}

## Evidence Status

| Item | Evidence Class | Result | Artifact |
| --- | --- | --- | --- |
| {THEN ID or gold test} | {class} | {PASS/FAIL/CANNOT_VERIFY/PENDING} | {path or none} |

## Warnings

| Level | Type | Message | Requires User Decision |
| --- | --- | --- | --- |
| {info/warning/debug} | {type} | {In the user's active language: message} | {yes/no} |

## User Decision Required

### Blocking

- {In the user's active language: decision that affects scope, acceptance, world model, evidence, or runtime reachability, or state that there is none}

### Non-Blocking

- {In the user's active language: deferred preference or polish decision, or state that there is none}

## Recent Events

- {timestamp}: {In the user's active language: event summary}
