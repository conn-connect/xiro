# Brief: {feature}

## Goal

{In the user's active language: one or two sentences describing what is being built, for whom, and why it matters.}

## Scope

- Scope mode: `{mockup-prototype | usable-local | production-ready}`
- In scope:
  - {In the user's active language: confirmed capability}
- Out of scope:
  - {In the user's active language: explicit non-goal}

## Users

- {In the user's active language: user type}: {In the user's active language: need or context}

## Must-Work Journeys

- {In the user's active language: journey}: {In the user's active language: end-to-end outcome the user must be able to perform}

## Key Decisions

- {In the user's active language: decision}: {In the user's active language: reason or source}

## Key Risks

- {In the user's active language: risk}: {In the user's active language: why it matters}

## Read Next

1. `state.md` - {In the user's active language: current honest status.}
2. `plan.md` - {In the user's active language: readable path to completion.}
3. `evidence/` - {In the user's active language: proof artifacts for claims.}
